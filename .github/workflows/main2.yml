name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run a hello world
        run: echo "Hello, world!"
        
      - name: Run a bad syntax
        run: echo "Hello, world!"; mv /foo/bar
